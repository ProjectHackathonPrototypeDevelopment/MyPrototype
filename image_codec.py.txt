# image_codec.py
# Placeholder for image compression

def compress_image(image_path str) - bytes
    
    Dummy image compressor for demo.
    
    with open(image_path, rb) as f
        raw = f.read()
    # Pretend to compress (truncate)
    return raw[5000]
